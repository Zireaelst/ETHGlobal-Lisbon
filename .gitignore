node_modules/
dist/
*.tsbuildinfo
.env
.env.*
!.env.example
*.log

# Next.js (web/)
web/.next/
web/out/
web/next-env.d.ts
*.tsbuildinfo

# Foundry
contracts/out/
contracts/cache/
contracts/broadcast/

# misc
.DS_Store
scripts/spikes/.hedera-x402-seller.json

# Claude Design handoff bundle — source reference only, already ported into packages/web
# (duplicates web/public/hero/*.png; kept local, not tracked)
landing-page/

# local AI-tooling artifacts (Claude Code skills cache, local settings) — not part of the submission
# skills-lock.json IS tracked (see README "Team setup") — it's a small manifest,
# not the vendored skill content, so teammates can restore the same skill set.
.claude/settings.local.json
.claude/skills/
.agents/
agent/

# Subgraph derleme çıktıları (kaynak: subgraph/src, subgraph.yaml, schema.graphql)
subgraph/generated/
subgraph/build/
