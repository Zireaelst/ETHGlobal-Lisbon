{
  "name": "@ca/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "//": "The demo dApp (P5-A). Its build chain is set up in P5 — currently outside the scope of tsc -b.",
  "scripts": {
    "build": "echo 'web: P5-A'de kurulacak' && exit 0"
  }
}
