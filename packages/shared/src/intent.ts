// intent.ts — EIP-712 domain/types, buildIntentHash, signIntent, recoverIntentSigner.
// intentHash = keccak256(abi.encode(briefHash, dataHash, constraintsHash, price, nonce))
// abi.encode — encodePacked DEĞİL (BUILD-PLAN P1-A fallback notu).
// P1-A'da implemente edilecek.
export {};
