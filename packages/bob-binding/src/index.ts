// @ca/bob-binding — the code that runs inside the enclave. Keep it MINIMAL: `imageHash` measures it.
// There is NO fraud switch, NO FRAUD_MODE and NO conditional behaviour here (BUILD-PLAN P1-D).
export * from './binding.js';
